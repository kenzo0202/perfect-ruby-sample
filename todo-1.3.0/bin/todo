#!/usr/bin/env ruby

require "bundler/setup"
require "todo"

Todo::Command.run(ARGV)

# (If you use this, don't forget to add pry to your Gemfile!)
# require "pry"
# Pry.start

require "pry"
Pry.start
